node_modules
dist
.env
.DS_Store
*.local

# the official assignment briefs — not ours to publish in the repo
info1.md
info2.md

# internal working notes (contains project refs / key references) — keep private
memory.md

# real people's session data with names — must not be public (handbook rule)
transcript-1.txt
transcript-2.txt

# hackathon handbook — not ours to redistribute
*.docx

# shared screenshots / scratch images — not app code
*.png
